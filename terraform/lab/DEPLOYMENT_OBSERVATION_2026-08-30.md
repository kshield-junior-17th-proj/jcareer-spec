# AWS 검증 Lab 재배포 관찰 — 2026-08-30

## 한눈에 보기

새 AWS 검증 계정에 사용할 자격 증명 경계를 확인한 뒤 HTTPS 미리보기와 Bedrock 연결을
포함한 Lab을 계획했다. Terraform 계획은 관리 리소스 `24개 생성, 변경 0개, 삭제 0개`로
검사를 통과했다. 적용은 네트워크 일부를 만든 뒤 런타임 IAM 역할 생성 권한이 없어 중단됐다.
부분 생성된 리소스는 별도의 저장된 삭제 계획으로 모두 정리했다. 2026-08-30 20:08 KST 기준
Lab Terraform 상태와 관련 네트워크·CloudFront 리소스는 0개다.

계정 번호, ARN, 리소스 ID, 미리보기 토큰과 Bedrock 응답 본문은 이 기록에 남기지 않았다.

## 관찰 결과

| 항목 | 결과 | 확인 범위 |
|---|---|---|
| 자격 증명 경계 | PASS | 의도한 새 계정 프로필인지 접두 경계로 확인. 식별자는 기록하지 않음 |
| Bedrock 직접 합성 호출 | PASS | 서울 리전, APAC Nova Lite, 입력 39토큰·출력 53토큰. 실제 지원자 정보 미사용 |
| Lab 저장 계획 | PASS | HTTPS 비공개 원본과 Bedrock 브로커 포함, 생성 24·변경 0·삭제 0 |
| Lab 적용 | BLOCKED | `iam:CreateRole` 권한 부재로 런타임 역할 생성 전에 중단 |
| 부분 적용 상태 확인 | PASS | 관리 리소스 16개와 읽기 전용 데이터 항목 3개를 식별자 없이 확인 |
| 복구용 삭제 계획 | PASS | 확인된 관리 리소스 16개만 삭제, 생성 0·변경 0 |
| 복구용 삭제 적용 | PASS | 저장한 동일 계획을 적용하고 Terraform 상태가 비었음을 확인 |
| 잔존 확인 | PASS | 관련 VPC·서브넷·NAT·공인 주소·CloudFront와 Terraform 상태 0 |

## 현재 상태

- `terraform/asis`의 2-AZ·6모듈·110개 모의 계획은 그대로이며 AWS에 적용한 구성이 아니다.
- 별도 `terraform/lab`은 현재 리소스 0개다. 미리보기 주소와 실행 중인 애플리케이션은 없다.
- Bedrock 결과는 계정에서 모델 호출이 가능하다는 직접 시험이다. API에서 브로커를 거치는 전체
  애플리케이션 경로의 성공을 뜻하지 않는다.
- MLOps는 기본 잠금 0개를 유지한다. AWS 배포나 Lambda 실행을 관찰하지 않았다.
- OpenDART 키를 확인하지 못해 외부 호출과 게시를 실행하지 않았다.
- 업무망 Windows 100대와 macOS 80대는 선언된 현황이며 단말 접속을 관찰하지 않았다.

## 재시도 조건

필요한 작업과 제한 조건은
[`IAM_RETRY_PREREQUISITES_2026-08-30.md`](IAM_RETRY_PREREQUISITES_2026-08-30.md)에
서비스별로 정리했다.

1. 새 계정의 배포 주체에 Lab 이름 범위로 제한한 IAM 역할·인스턴스 프로필 생성 및 관리,
   정책 연결과 `iam:PassRole` 권한을 부여한다.
2. 새 임시 접속 토큰을 만들고 `24개 생성, 변경 0개, 삭제 0개` 계획을 다시 검토한다.
3. 저장한 동일 계획만 적용한다.
4. 원격 여섯 서비스, HTTPS 인증 경계, Bedrock 전체 경로를 시험한다.
5. 배포 직후 Unix 소켓 디렉터리·소켓 소유권과 전체 경로를 확인한다. 재부팅 지속성은
   별도 관찰 항목으로 남긴다.

권한 부여는 계정 관리자가 결정할 항목이다. 이 기록은 권한 확대나 운영 승인을 대신하지 않는다.
